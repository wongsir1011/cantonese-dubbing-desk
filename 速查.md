# 速查卡

## 開機

雙擊 **`launch.bat`**（Windows）或 **`launch.command`**（Mac）。瀏覽器會自動開。
用完喺黑色視窗按 `Ctrl+C`。

四個檔案必須擺埋同一個資料夾：`index.html`、`serve.py`、`launch.bat`、`launch.command`。

---

## 第一次設定

撳右上角「設定」，填四樣（兩個服務）：

| 欄位 | 填咩 | 喺邊度攞 |
|---|---|---|
| Azure Speech Key | KEY 1 | Azure 資源頁 → Keys and Endpoint |
| Azure 區域 | `southeastasia` | 同一頁嘅 Location/Region，細楷冇空格 |
| Azure 資源名稱 | `wongsir1011` | 資源頁左上角個大字 |
| Google AI Studio Key | `AIza...` | [aistudio.google.com](https://aistudio.google.com) → Get API key |



翻譯模型預設 `gemini-2.5-flash`：免費層每分鐘 10 次、每日 250 次，一集 20 分鐘節目約 2 分鐘譯完。譯文如果偏書面，用校對頁嘅「重譯有問題嘅句子」修。

其餘留返預設就得。金鑰只存喺你部瀏覽器，換部機要重新填。

**可選 — MiniMax 語音**：想試更自然嘅粵語聲，去 [platform.minimax.io](https://platform.minimax.io) 開戶攞 API Key 同 Group ID（帳戶管理頁兩樣都有），填落設定，「語音引擎」轉「MiniMax 官方」。按量收費，一集 20 分鐘節目約 US$0.2–0.4。預設用官方粵語主持聲。轉錄仍然行 Azure，唔受影響。

**可選 — 用真人聲配音**：揀咗 MiniMax 語音之後，設定最底有「聲音克隆」。上載一段 10 秒–5 分鐘嘅錄音（mp3／m4a／wav，≤20MB），改個英文 voice ID，撳「克隆呢把聲」，成功後撳「用做男聲／女聲」就用得。⚠️ **只可以用自己把聲或者已攞到當事人同意嘅人**；克隆出嚟嘅聲 **7 日內冇用過會被自動刪走**。詳細步驟見使用說明書第 8 章。

---

## 日常流程

```
拖檔案入去  →  開始轉錄  →  （自動翻譯）  →  校對  →  合成
```

**音檔**（mp3/m4a/wav）→ 出廣東話 MP3
**影片**（mp4/mov/webm）→ 出配音 MP4 + 字幕

源語言可以係任何語言。設定入面「**片源語言**」預設自動偵測，唔肯定就唔使郁；
知道條片係咩語言就直接指定，會準啲快啲。

---

## 校對頁七粒掣

| 掣 | 做咩 | 幾時用 |
|---|---|---|
| 顯示原文 | 對照原文原句 | 覺得譯錯意思 |
| 匯出稿件 | 純文字稿 | 想另外存檔 |
| 匯出 SRT | 廣東話字幕檔 | 淨係要字幕 |
| 用呢個語氣重譯 | 換語氣重譯全部 | 語氣唔啱 |
| 稿件轉繁體 | 原文譯文一齊轉繁 | 見到簡體字 |
| **重譯書面語句子** | 只重譯標黃嗰啲 | **譯文太書面** |
| 合成廣東話音檔 | 落一步 | 校對完 |

句子左邊係**黃色**兼有「書面語」標籤 = 呢句唔夠口語。撳「重譯書面語句子」只會動呢啲。

每句仲有：`男A/女B`（單獨調換）、`▶`（試聽）、`⇅`（由呢句起全部調轉男女）。

---

## 三個常見情況

**譯文似書面語**
撳「重譯有問題嘅句子」。連撳兩次通常會再好啲——每次結果唔同，只會保留有改善嘅譯文。

**合成語音一路 Failed to fetch**
設定 → Azure 定價層 → 確認揀咗「F0 免費層」，程式會自動放慢。

**影片同聲對唔上**
設定 → 聲畫對齊方式 → 預設「壓縮音軌」。壓到盡都仲超出就揀「延長影片」。

---

## 頁面頂出現黃色框？

自檢已經寫咗係咩事，照做就得。最常見係：

- 雙擊咗 `index.html` → 應該雙擊 `launch.bat`
- 部署咗上 Vercel / GitHub Pages → 呢個程式只可以喺本機行

---

## 免費額度

| | 額度 | 你一集用幾多 |
|---|---|---|
| Azure F0 | 每月 5 小時轉錄、50 萬字元語音 | 20 分鐘節目約 8000 字元 |
| Gemini 免費層 | 每日 250 次請求 | 約 14 次 |

即係一個月大約做到十五集 20 分鐘節目，完全免費。

校對頁重譯唔會用轉錄額度，只用 Gemini 請求，可以放心試唔同語氣。

---

## 轉錄引擎點揀

設定入面「轉錄引擎」三揀一：

- **Azure 快速轉錄**（預設）— 免費夠用，時間碼準。Azure 額度爆咗先考慮換
- **ElevenLabs Scribe v2** — 時間碼最準、講者標籤貫穿全片（因為唔切段）。約 US$0.22 一個鐘，冇免費層
- **Gemini 音頻理解** — 用返翻譯果個 key，唔使另外開戶。⚠ 時間碼係估出嚟，長片會飄，做影片對嘴要小心

換引擎唔使改其他嘢，流程一樣。

ElevenLabs 永遠自己偵測語言，唔受「片源語言」設定影響。

---

## 語音引擎點揀

| 引擎 | 粵語聲 | 收費 | 幾時用 |
|---|---|---|---|
| **Azure zh-HK**（預設）| 3 把 | F0 免費層夠用 | 一般情況 |
| **Google Chirp 3: HD** | **30 把** | 按量 | 想要多啲音色、想聲底自然啲 |
| MiniMax 官方 | 官方粵語主持一男一女 | 按量 | 想試克隆真人聲 |
| GLM-TTS | 冇官方粵語 | 按量 | 實驗性質 |

用 Chirp 3: HD 之前，條 Google key 要喺 Cloud Console 做兩件事：開啟
**Cloud Text-to-Speech API**，同埋喺 key 嘅 API restrictions **同時**勾住
Generative Language API（翻譯用）同 Cloud Text-to-Speech API。淨係勾一個，另一樣會壞。
